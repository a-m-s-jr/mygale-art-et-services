{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "bun run build --cwd apps/web",
  "installCommand": "bun install",
  "outputDirectory": "apps/web/.next",
  "rewrites": [{ "source": "/api/:path*", "destination": "/api/:path*" }],
  "env": {
    "NODE_ENV": "production"
  }
}
